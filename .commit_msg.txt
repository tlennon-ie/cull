brand: refreshed dark logo with glow, all dashboard screenshots reshot

* assets/logo-transparent-dark.png
  Replaced with the new high-resolution variant the user generated
  (2404x2404 RGBA with a subtle glow halo around the C). Compressed
  in place via Pillow Octree at 128 colours so the glow gradient
  survives without dithering artefacts. 508 KB -> 61 KB (88% reduction)
  with the full resolution preserved - looks crisp at the 32px nav
  size and at the 96px About-card size, and remains usable as a
  high-fidelity social card export.

* docs/screenshots/{overview,stats,gallery,scrapers,about,faq}.png
  All six dashboard screenshots reshot against the cull-branded
  build at 1440x900: page title now shows "cull", the funnel logo
  (with glow) sits in the sidebar nav, FAQ + About tabs are visible,
  the brand footer line ("cull · MIT · github.com/tlennon-ie/cull")
  is at the bottom of every page. The Gallery + Stats screenshots
  carry the same data as before (35 mock items via seed_demo_data.py)
  so the visual continuity from the prior commit holds.

* pipeline_code/dashboard_enhanced.py
  Updated the About-tab Brand card prose to point at the actual
  assets/ folder + the live /brand/<filename> endpoint, with three
  inline links to each PNG variant. The previous prose still
  referenced docs/brand/ which doesn't exist - fixed to match
  reality.

* README.md
  Dashboard preview table extended to a third row showing About +
  FAQ. Tab count corrected from "Nine tabs" to "Eleven tabs"
  (Overview, Stats, Gallery, Scrapers, Vision, Queue, Historical,
  Errors, Settings, FAQ, About).

Verified by re-shooting all screenshots after the dashboard restart
picked up the prose change. Demo dashboard cleanly stopped after
capture; data/ still gitignored.
